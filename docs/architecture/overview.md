# Architecture Overview

Status: `Draft`

`vibepublish` is currently a scaffold. Architecture decisions should be added here only after the related feature requirements are documented.

Current committed component:

- `llm_gateway`: Google AI / Supabase limit-control framework.

