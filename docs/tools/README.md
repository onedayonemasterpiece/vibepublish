# Tools

Status: `Draft`

Useful local commands and helper scripts should be documented here.

## LLM Gateway

```bash
python scripts/inspect/probe_supabase_rpc.py google_ai_reserve --schema public --use-service
python scripts/inspect/sweep_google_ai_stale.py --schema public --use-service
```

